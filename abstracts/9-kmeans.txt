# 9-kmeans abstract

This chapter shows how to examine the clusters derived from k-means clustering
visually. This can be used in conjunction with summary statistics, to decide on
the optimal number of clusters. It first describes the approach to overlaying the 
model statistics on the data in two dimensions, followed by an explanation of 
how this can be extended into the high-dimensional setting when using tours. 
